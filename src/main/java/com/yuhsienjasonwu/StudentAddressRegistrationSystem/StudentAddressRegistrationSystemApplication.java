// Declare the package of the class
package com.yuhsienjasonwu.StudentAddressRegistrationSystem;

// Import the necessary Spring Boot classes
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Declare the main application class, which bootstraps the Spring Boot application
@SpringBootApplication
public class StudentAddressRegistrationSystemApplication {

	// Declare the main method, which launches the Spring Boot application
	public static void main(String[] args) {
		SpringApplication.run(StudentAddressRegistrationSystemApplication.class, args);
	}

}

